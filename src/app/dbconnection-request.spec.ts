import { DBConnectionRequest } from './dbconnection-request';

describe('DBConnectionRequest', () => {
  it('should create an instance', () => {
    expect(new DBConnectionRequest()).toBeTruthy();
  });
});
